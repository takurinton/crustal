[package]
name = "crustal-macros"
version = "0.1.0"
edition = "2021"

[dependencies]
proc-macro2 = "1.0"
quote = "1.0"
syn = { version = "2", features = ["full", "extra-traits", "visit-mut"] }

[dev-dependencies]
crustal-markdown = { path = "../crustal-markdown" }

[lib]
proc-macro = true
